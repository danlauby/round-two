// ----PHP-----
//drupal_add_js(array'module_name) =>array(jsVAriable => phpVariable)), array('type' =>'setting');
//drupal_add_js(drupal_getpath('module', 'module_name') . 'scripts.js');




// -----JS//-------
// (function($) {
    // Drupal.behaviors.module_name = {
    // attach: function(context, settings) {
    //      var variable = settings.module_name.jsVariable;
    //      ** Code stays here
    // }
    // }
// })(jQuery);
